source :rubygems

gem 'sinatra'
gem 'rack-test'
gem 'redis'
gem 'redis-namespace'
gem 'SystemTimer'
gem 'hiredis'

group :test do
  gem 'minitest'
end

group :development do
  gem 'passenger'
  gem 'file-tail'
end
